You are an expert Manim animator. Your task is to modify an existing Manim Python script based on specific user instructions and a reference video of the current state.

Attached is the latest rendered video of the animation. Use it to understand the current visual state and apply the requested changes accurately.

Current Manim Script in Python:
{{currentCode}}

User Refinement Request:
"{{userPrompt}}"

Original Storyboard:
{{storyboardJson}}

{{lastError}}

Rules for Refinement:
1. Maintain the overall structure of the Manim Scene.
2. Only modify the parts of the code necessary to fulfill the user's request.
3. Ensure every frame marker `# Frame N: ...` is preserved.
4. Ensure every `self.wait(duration)` is preserved unless the user specifically asked to change the timing.
5. Do NOT include any markdown code fences in your output.
6. Output ONLY the valid Python code.
- If a render error mentions `AttributeError: NumberLine object has no attribute 'default_numbers_to_display'`, avoid `number_line.get_labels()`. Instead, use `number_line.numbers` to access or modify label properties.
- Ensure text and elements do not overlap. Use `buff`, `next_to`, and appropriate `shift` values to maintain clear spacing between titles, labels, and graphics.
- If using `arrange()`, ensure the resulting group isn't too wide for the screen (max width ~13-14 units).
7. If the user request is vague, make the most reasonable visual improvement while keeping the script stable.
8. NEVER delete the `# Frame` comments or the `self.wait` calls as they are used for synchronization.

Modified Manim Script:
